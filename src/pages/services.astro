---
export const prerender = true;

import "../styles/global.css";
import Header from "@/components/header";
import Footer from "@/components/footer";
import { ServiceCard } from "@/components/service-card";
import { data } from "@/components/carousel-accordion-section/data";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Services | Cleverli</title>
  </head>
  <body
    class="flex min-h-screen flex-col items-center gap-8 overflow-x-hidden bg-linear-to-br from-slate-50 to-slate-100 lg:p-8"
  >
    <Header client:load pathname={Astro.url.pathname} />
    <div
      class="flex w-full max-w-360 flex-col gap-10 p-4 pb-0 lg:gap-20 lg:px-0"
    >
      <p
        class="font-frutiger mx-auto text-center text-[24px] font-normal text-slate-800 italic lg:mt-32 lg:max-w-5xl lg:text-[40px] lg:leading-12"
      >
        We design and deliver custom e-learning solutions, combining content,
        visuals, and technology to create engaging, effective learning
        experiences.
      </p>

      <!-- Service Cards -->
      <div class="mt-12 flex flex-col gap-6 lg:gap-10">
        {
          data.map(service => (
            <ServiceCard
              title={service.title}
              items={service.details}
              imageSrc={service.src}
              imageAlt={service.title}
            />
          ))
        }
      </div>
    </div>
    <Footer client:load />
  </body>
</html>
